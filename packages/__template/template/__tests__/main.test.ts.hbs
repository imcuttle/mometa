{{#if useTs}}
  import
  {{camelCase name}}
  from '../src'
{{else}}
  const
  {{camelCase name}}
  = require('../src')
{{/if}}

describe('{{camelCase name}}', function() { it( 'spec case', function () { } ) })